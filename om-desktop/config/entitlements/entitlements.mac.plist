<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow JIT compilation for performance -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for Node native modules) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (required for third-party native modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Microphone access (for audio recording) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Screen recording/capture (CRITICAL for meeting recording) -->
    <key>com.apple.security.device.capture</key>
    <true/>

    <!-- Apple Events automation (for window detection) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Standard Electron entitlements -->
    <key>com.apple.security.device.bluetooth</key>
    <true/>
    <key>com.apple.security.device.print</key>
    <true/>
    <key>com.apple.security.device.usb</key>
    <true/>
    <key>com.apple.security.personal-information.location</key>
    <true/>
  </dict>
</plist>
